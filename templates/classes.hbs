<!DOCTYPE html>
    <head>
        <meta charset="UTF-8" />
        <title>{{product}} Class Reference</title>
    </head>
    <body>
        <section>
            <nav>{{#each files}}
                <a href="{{_prefix}}">{{name}}</a>
            {{/each}}</nav>
        </section>
    </body>
</html>