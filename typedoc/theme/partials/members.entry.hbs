<article class="{{CamelCase cssClasses}}">

    <a name="{{anchor}}"></a>

    {{#if name}}
        <h3>{{#each flags}}<span class="Flag{{CamelCase this}}">{{this}}</span> {{/each}}{{wbr name}}</h3>
    {{/if}}

    {{> members.comment}}

    {{> members.aside}}

</article>
